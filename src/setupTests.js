import "@testing-library/react/cleanup-after-each";
import "@testing-library/jest-dom/extend-expect";//The project boilerplate is configured to extend the default matchers with more specific matchers that help test the DOM. The src/setupTests.js file configures Jest to use jest-dom.
